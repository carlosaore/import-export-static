const users = ["Jana", "Pierre", "Gauthier", "Tim"];

/**
 * Logs the text provided as argument
 * @param {String} text 
 */
const logger = (text) => {
    console.log(text)
}

export { users, logger }